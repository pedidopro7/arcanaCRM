import { NextResponse } from 'next/server';
import { google } from 'googleapis';
import { Readable } from 'node:stream';
import { getSupabaseAdmin } from '@/lib/supabase-admin';
import { decryptSecret } from '@/lib/crypto';

async function findOrCreateFolder(drive:ReturnType<typeof google.drive>,name:string,parentId:string){const safe=name.replace(/'/g,"\\'");const found=await drive.files.list({q:`name='${safe}' and mimeType='application/vnd.google-apps.folder' and '${parentId}' in parents and trashed=false`,fields:'files(id,name)',pageSize:1});if(found.data.files?.[0]?.id)return found.data.files[0].id;const created=await drive.files.create({requestBody:{name,mimeType:'application/vnd.google-apps.folder',parents:[parentId]},fields:'id'});if(!created.data.id)throw new Error('Falha ao criar pasta');return created.data.id}

export async function POST(req:Request){try{const supabase=getSupabaseAdmin();if(!supabase)return NextResponse.json({error:'Supabase não configurado'},{status:503});const h=req.headers.get('authorization')||'';const token=h.startsWith('Bearer ')?h.slice(7):'';if(!token)return NextResponse.json({error:'Não autenticado.'},{status:401});const {data:{user},error:userError}=await supabase.auth.getUser(token);if(userError||!user)return NextResponse.json({error:'Sessão inválida.'},{status:401});const {data:member}=await supabase.from('organization_members').select('organization_id').eq('user_id',user.id).limit(1).maybeSingle();if(!member)return NextResponse.json({error:'Usuário sem workspace.'},{status:403});const form=await req.formData();const file=form.get('file');if(!(file instanceof File))return NextResponse.json({error:'Arquivo obrigatório'},{status:400});if(file.size>50*1024*1024)return NextResponse.json({error:'Arquivo acima de 50 MB. Use o Drive diretamente para arquivos maiores.'},{status:413});const client=String(form.get('client')||'Sem cliente'),campaign=String(form.get('campaign')||'Geral'),creator=String(form.get('creator')||'Geral'),category=String(form.get('category')||'Arquivos');const {data:integration}=await supabase.from('integrations').select('*').eq('organization_id',member.organization_id).eq('provider','google_workspace').eq('status','connected').maybeSingle();if(!integration?.refresh_token_encrypted)return NextResponse.json({error:'Google Drive não conectado'},{status:409});const oauth2=new google.auth.OAuth2(process.env.GOOGLE_CLIENT_ID,process.env.GOOGLE_CLIENT_SECRET,process.env.GOOGLE_REDIRECT_URI);oauth2.setCredentials({refresh_token:decryptSecret(integration.refresh_token_encrypted)});const drive=google.drive({version:'v3',auth:oauth2});const root=integration.metadata?.root_folder_id||process.env.GOOGLE_DRIVE_ROOT_FOLDER_ID;if(!root)return NextResponse.json({error:'Pasta raiz não configurada'},{status:409});const clientFolder=await findOrCreateFolder(drive,client,root),campaignFolder=await findOrCreateFolder(drive,campaign,clientFolder),creatorFolder=await findOrCreateFolder(drive,creator,campaignFolder),categoryFolder=await findOrCreateFolder(drive,category,creatorFolder);const uploaded=await drive.files.create({requestBody:{name:file.name,parents:[categoryFolder]},media:{mimeType:file.type||'application/octet-stream',body:Readable.from(Buffer.from(await file.arrayBuffer()))},fields:'id,name,webViewLink,mimeType,size'});const nullable=(v:FormDataEntryValue|null)=>{const s=String(v||'').trim();return s||null};await supabase.from('files').insert({organization_id:member.organization_id,client_id:nullable(form.get('client_id')),campaign_id:nullable(form.get('campaign_id')),influencer_id:nullable(form.get('influencer_id')),name:uploaded.data.name||file.name,category,provider:'google_drive',external_id:uploaded.data.id,external_url:uploaded.data.webViewLink,mime_type:uploaded.data.mimeType,size_bytes:uploaded.data.size?Number(uploaded.data.size):file.size,created_by:user.id});await supabase.from('activity_logs').insert({organization_id:member.organization_id,actor_user_id:user.id,event_type:'file_uploaded',description:`Arquivo ${file.name} enviado ao Google Drive.`,metadata:{category,external_id:uploaded.data.id}});return NextResponse.json({ok:true,file:uploaded.data})}catch(error){console.error('drive_upload_error',error);return NextResponse.json({error:'Falha no upload para o Drive'},{status:500})}}
