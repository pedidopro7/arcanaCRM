import { NextResponse } from 'next/server';
import { z } from 'zod';
import { getSupabaseAdmin } from '@/lib/supabase-admin';

const schema=z.object({email:z.string().email(),password:z.string().min(8),name:z.string().min(2).max(120),role:z.enum(['admin','manager','operator','finance','viewer']).default('operator')});
async function authorize(req:Request){const admin=getSupabaseAdmin();if(!admin)return {error:NextResponse.json({error:'Supabase não configurado.'},{status:503})};const h=req.headers.get('authorization')||'';const token=h.startsWith('Bearer ')?h.slice(7):'';if(!token)return {error:NextResponse.json({error:'Não autenticado.'},{status:401})};const {data:{user},error}=await admin.auth.getUser(token);if(error||!user)return {error:NextResponse.json({error:'Sessão inválida.'},{status:401})};const {data:member}=await admin.from('organization_members').select('organization_id,role').eq('user_id',user.id).limit(1).maybeSingle();if(!member)return {error:NextResponse.json({error:'Usuário sem workspace.'},{status:403})};if(!['admin','manager'].includes(member.role))return {error:NextResponse.json({error:'Sem permissão.'},{status:403})};return {admin,member}}
export async function GET(req:Request){const auth=await authorize(req);if('error'in auth)return auth.error;const {admin,member}=auth;const {data:list,error}=await admin.auth.admin.listUsers({page:1,perPage:200});if(error)return NextResponse.json({error:error.message},{status:500});const ids=list.users.map(u=>u.id);const {data:members}=ids.length?await admin.from('organization_members').select('user_id,role').eq('organization_id',member.organization_id).in('user_id',ids):{data:[] as {user_id:string;role:string}[]};const map=new Map((members||[]).map(m=>[m.user_id,m.role]));return NextResponse.json({users:list.users.filter(u=>map.has(u.id)).map(u=>({id:u.id,email:u.email,name:u.user_metadata?.name||u.user_metadata?.full_name||u.email?.split('@')[0]||'Usuário',role:map.get(u.id)||'operator',created_at:u.created_at,last_sign_in_at:u.last_sign_in_at}))})}
export async function POST(req:Request){const auth=await authorize(req);if('error'in auth)return auth.error;const {admin,member}=auth;const parsed=schema.safeParse(await req.json().catch(()=>null));if(!parsed.success)return NextResponse.json({error:'Dados inválidos.'},{status:400});const {email,password,name,role}=parsed.data;const {data,error}=await admin.auth.admin.createUser({email:email.toLowerCase().trim(),password,email_confirm:true,user_metadata:{name}});if(error||!data.user)return NextResponse.json({error:error?.message||'Falha ao criar usuário.'},{status:400});const {error:mError}=await admin.from('organization_members').upsert({organization_id:member.organization_id,user_id:data.user.id,role},{onConflict:'organization_id,user_id'});if(mError)return NextResponse.json({error:mError.message},{status:500});return NextResponse.json({ok:true},{status:201})}
